#include "functions.h"

// non-const static variable initialisation must be made outside a .h file and inside a .cpp file
// and outside the scope of the class definition
int GoingToBeInherited::n_members = 0;
